name: projeto_cursor_web
description: Projeto Flutter web básico criado pelo Cursor
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Suporte a Flutter Web já vem no SDK padrão
  cupertino_icons: ^1.0.8
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
